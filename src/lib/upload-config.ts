/** Shared upload configuration (used on both client and server) */
export const ALLOWED_EXTENSIONS = [".pdf", ".jpg", ".jpeg", ".png", ".tiff", ".docx"];
export const MAX_FILE_SIZE = 50 * 1024 * 1024; // 50MB
