# This is the updated content for the main_hybridai-next-react-vite.yml file.
# Updated push branch trigger from master to main.

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      # other steps here
